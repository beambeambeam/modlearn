# Better Auth Configuration
# Generate a secure secret key for production
# You can use: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-key-here-change-in-production

# URL where Better Auth service runs
# This is your backend server URL
BETTER_AUTH_URL=http://localhost:3000

# CORS Configuration
# URL of your frontend application
CORS_ORIGIN=http://localhost:3001

# Database Configuration
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
# For Docker setup (default):
DATABASE_URL=postgresql://postgres:password@localhost:5432/modlearn
# For local PostgreSQL:
# DATABASE_URL=postgresql://postgres:yourpassword@localhost:5432/modlearn

# RustFS S3-Compatible Storage Configuration
# Used for storing media files, thumbnails, and user profile images

# S3 API endpoint (default: http://localhost:9000 for Docker RustFS)
S3_ENDPOINT=http://localhost:9000

# Access credentials for RustFS
# Development default: rustfsadmin / rustfsadmin
# SECURITY WARNING: Change these in production! Generate with: openssl rand -base64 32
S3_ACCESS_KEY_ID=rustfsadmin
S3_SECRET_ACCESS_KEY=rustfsadmin

# Bucket name for storing media files
# Must be lowercase alphanumeric with hyphens, 3-63 characters
S3_BUCKET_NAME=modlearn-media

# AWS S3 region (RustFS ignores this but required for S3 clients)
S3_REGION=us-east-1

# Public URL for accessing files (CDN or direct access)
# For development: same as S3_ENDPOINT
# For production: use CDN URL (e.g., https://cdn.example.com)
S3_PUBLIC_URL=http://localhost:9000
